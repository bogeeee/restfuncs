{
  "name": "restfuncs",
  "version": "0.8.0",
  "description": "Test that covers client and server code at the same time",
  "type": "module",
  "scripts": {
    "dev": "nodemon -e ts --exec npm run compile_and_run_tests",
    "compile_and_run_tests": "clear && tsc --build && jest",
    "clean": "tsc --build --clean && cd client && npm run clean && cd .. && cd server && npm run clean",
    "build": "tsc --build --force && cd client && npm run build && cd .. && cd server && npm run build",
    "prepublish": "npm run clean && npm run build"
  },
  "workspaces": ["server", "client", "examples/vite_separated/server", "examples/vite_separated/client"],
  "devDependencies": {
    "ts-jest": "^29.0.3",
    "@types/jest": "^29.2.2",
    "typescript": "^4.8.4"
  },
  "keywords": [],
  "author": "",
  "license": "MIT"
}
