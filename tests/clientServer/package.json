{
  "name": "restfuncs-clientserver-tests",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev:fastbuild": "tspc --build",
    "dev:build_tsc_only": "ts-patch uninstall --silent && tsc",
    "clean": "tsc --build --clean",
    "test": "jest --config jest.config.js --runInBand"
  },
  "dependencies": {
    "express": "^4.17.13",
    "@types/express": "^4.17.13",
    "vite": "4.1.4",
    "restfuncs-server": "../../server",
    "restfuncs-client": "../../client"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "tsx": "^4.7.0",
    "ts-patch": "^3.0.2",
    "nodemon": "^2.0.15",
    "cross-env": "^7.0.3"
  }
}
