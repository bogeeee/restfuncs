{
  "name": "restfuncs-example",
  "version": "2.0.0",
  "description": "",
  "type": "module",
  "main": "./server",
  "scripts": {
    "dev": "npm run clean && nodemon --exec cross-env NODE_ENV=development DISABLESECURITY=true tsx server.ts",
    "dev:with_validation": "nodemon -e ts --ignore client --exec \"clear && tspc --build && cross-env NODE_ENV=development node --enable-source-maps server.js\"",
    "clean": "tspc --build --clean && rm -rf client/dist",
    "build": "tspc --build --force && cd client && vite build",
    "start": "node --enable-source-maps server.js"
  },
  "dependencies": {
    "express": "^4.17.13",
    "@types/express": "^4.17.13",
    "vite": "4.1.4",
    "restfuncs-server": "^3.0.0",
    "restfuncs-client": "^2.0.0",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "ts-patch": "^3.0.2",
    "restfuncs-transformer": "^1.0.0",
    "nodemon": "^2.0.15",
    "cross-env": "^7.0.3"
  }
}
